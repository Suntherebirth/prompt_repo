z# Prompt Repo Design Guide

이 문서는 기능을 건드리지 않고, 디자인 방향만 정리한 가이드입니다. 현재 앱은 어두운 다크 테마 기반의 작업형 인터페이스로 시작되어 있고, 향후 UI 리디자인은 이 톤을 유지하면서 가독성, 계열감, 시각적 우선순위를 강화하는 방향으로 진행하는 것을 목표로 합니다.

## 1. 디자인 원칙

### 1-1. 기본 방향
- 다크 모드 우선: 배경은 저명도, 텍스트는 높은 대비로 유지
- 정보 계층 명확화: 카드, 섹션, 버튼, 상태 표시가 구분되도록 설계
- 기능 기반 컬러 분리: 개별 프롬프트, 조합, 콤보, 태그를 색상 시스템으로 구분
- 과하지 않은 강조: 포인트 컬러는 UI의 액션과 상태를 표현하는 역할로만 사용
- 구성 요소는 모듈형: 카드, 배지, 버튼, 입력영역은 반복 가능한 패턴으로 정리

### 1-2. 디자인 톤
- 무드: 기술적, 정리된, 세련된, 협업형 생산성 도구
- 느낌: 네온 보다는 심플한 디지털, 고급스러운 다크 톤
- 중점: 작업 효율성, 정보 탐색, 빠른 인식

---

## 2. 기본 디자인 토큰

### 2-1. Neutral palette

| 토큰 | 용도 | 색상 |
| --- | --- | --- |
| bg-base | 앱 기본 배경 | #0F1117 |
| bg-surface | 카드/패널 배경 | #171B22 |
| bg-elevated | 선택/호버/상위 레벨 카드 | #1E2430 |
| bg-muted | 비활성 영역, 분리선 | #2A3140 |
| border-subtle | 얇은 보더 | #3A4356 |
| border-strong | 중요 보더, 상태 강조 | #4F5B75 |
| text-primary | 주요 텍스트 | #F3F6FB |
| text-secondary | 보조 텍스트 | #B9C3D6 |
| text-muted | 약한 텍스트 | #7E8AA3 |
| text-inverse | 버튼/강조 배경 위 텍스트 | #FFFFFF |

### 2-2. 공통 액션 색상

| 토큰 | 용도 | 색상 |
| --- | --- | --- |
| action-primary | 주요 액션, 버튼 강조 | #4C5BD6 |
| action-primary-strong | hover/active 강조 | #7484E8 |
| action-secondary | 보조 액션 | #5DA8FF |
| success | 완료/활성 상태 | #34D399 |
| warning | 주의 상태 | #F59E0B |
| danger | 삭제/위험 상태 | #F87171 |
| info | 정보 레벨 | #38BDF8 |

### 2-3. 반투명 레이어
- 라이트 오버레이: rgba(255,255,255,0.04)
- 포인트 오버레이: rgba(139,92,246,0.12)
- 강조 오버레이: rgba(93,168,255,0.14)
- 상태 오버레이: rgba(52,211,153,0.12)

### 2-4. 그림자
- shadow-soft: 0 8px 20px rgba(0,0,0,0.18)
- shadow-card: 0 10px 24px rgba(15,17,23,0.28)
- shadow-focus: 0 0 0 2px rgba(139,92,246,0.35)

### 2-5. 추가 기능 버튼 규칙
 버튼 배경은 각 기능 컬러셋의 기본 강조색보다 한 단계 어두운 색을 사용해 네온처럼 튀지 않게 조정
---

## 3. 타이포그래피

### 3-1. 기본 계열
- 본문: Segoe UI / Inter / system-ui
- 제목: 보통 600~700 weight
- 숫자/메타 정보: 가독성을 위해 medium 또는 semibold 사용

### 3-2. 텍스트 계층
- H1: 1.5rem / 700
- H2: 1.2rem / 700
- H3: 1rem / 600
- Body: 0.95rem / 400~500
- Caption: 0.75rem / 500

### 3-3. 행간/간격 규칙
- 카드 내부 padding: 12~16px
- 섹션 간 간격: 16~24px
- 버튼 높이: 36~42px
- 아이콘 배치 간격: 8px 단위 사용

---

## 4. 컴포넌트 스타일 방향

### 4-1. 카드
- 배경: bg-surface
- 보더: 1px solid border-subtle
- radius: 12~16px
- hover 시: 배경 살짝 밝아짐 + border 강화 + 약한 그림자

### 4-2. 버튼
- 기본: 배경 어두운 톤 + 경계선
- 주요 액션: 보라 계열 포인트 컬러
- 보조 액션: 파랑 계열 포인트 컬러
- 비활성: 투명도 낮은 배경 + 낮은 대비 텍스트

카테고리 칩 상태:
- 개별 프롬프트 카테고리 칩의 기본/hover/active/used 상태는 프롬프트 인디고 계열 안에서만 변주
- 현재 선택된 칩은 밝은 경계선과 약한 링으로 구분하고, 사용 중인 칩은 낮은 대비의 인디고 배경으로 표시

### 4-3. 배지 / 태그
- 라운드 처리
- 텍스트는 배경보다 더 밝은 톤 사용
- 배경은 항목별 의미 색상 기반

### 4-4. 입력 영역
- 배경은 bg-elevated 수준
- 보더는 기본 상태에서 subtle, focus 시 강한 보라/파랑 강조
- placeholder는 text-muted

---

## 5. 컬러셋 가이드

아래 컬러는 각각 의미를 가진 데이터 유형을 시각적으로 분리하기 위한 기본 세트입니다. 기능은 바꾸지 않고, 디자인적으로만 계열을 정리하는 단계입니다.

### 5-1. 개별 프롬프트 관련 컬러셋
개별 프롬프트는 하나의 기본 컬러 라인으로 통일합니다. 편집 상태, 선택 상태, 강조 상태는 모두 동일한 인디고 계열에서 명도/채도만 조절해 표현하고, 다른 계열 색상으로 넘어가지 않습니다.

| 토큰 | 의미 | 색상 |
| --- | --- | --- |
| prompt-base | 기본 프롬프트 컬러 | #4C5BD6 |
| prompt-strong | 프롬프트 강조/선택/상위 프라이오리티 | #7484E8 |
| prompt-surface | 프롬프트 카드 배경 | #1C213B |
| prompt-border | 프롬프트 영역 경계 | #3A4479 |
| prompt-soft | 연한 강조 배경 | rgba(76,91,214,0.12) |
| prompt-text | 프롬프트 관련 텍스트 | #E1E8FF |

사용 가이드:

### 5-1-1. 개별 프롬프트 색상 원칙
- 기본 컬러는 인디고 계열로 통일: #4C5BD6
- 상태 변화는 같은 색상 라인에서 tint/shade 변주만 적용
- 인디고 계열 외의 색을 개별 프롬프트 강조로 쓰지 않음
- 선택/편집/활성 상태는 동일한 컬러 계열 내에서 대비를 조정
- 조합된 프롬프트 안의 버튼식 프롬프트 스택도 개별 프롬프트와 같은 인디고 계열을 사용

### 5-2. 커스텀 조합 관련 컬러셋
커스텀 조합은 구조적/논리적 연결과 완성된 결과물의 묶음을 강조하는 muted rose-wine 계열로 설계합니다. 개별 프롬프트의 인디고, 태그의 골드와 충분히 구분하면서도 다크 화면에서 차분하고 고급스럽게 보이는 것이 목적입니다.

| 토큰 | 의미 | 색상 |
| --- | --- | --- |
| combo-primary | 조합 기본 강조 | #A95773 |
| combo-secondary | 보조 강조 | #D27D98 |
| combo-surface | 조합 카드 배경 | #2B1D27 |
| combo-border | 조합 구분선 | #63384B |
| combo-soft | 연한 강조 배경 | rgba(169,87,115,0.14) |
| combo-text | 조합 관련 텍스트 | #F1D5DE |

사용 가이드:
- 커스텀 조합 리스트, 조합 통계, 결과 그룹핑, 활성 상태 등에 사용
- 로즈 와인 계열은 구조적 연결과 완성된 묶음의 느낌을 주되, 위험 상태의 레드보다 어둡고 부드럽게 유지
- 커스텀 조합 탭의 카테고리 칩과 중앙 조합 아이템 카드에도 동일한 로즈 와인 계열을 적용
- 커스텀 조합의 일반/편집용 전환 버튼과 상단 조합 제목도 동일한 로즈 와인 계열로 연결
- 현재 조합을 저장하는 `새 조합` 버튼은 커스텀 조합의 주요 액션으로 로즈 와인 계열을 사용
- 프롬프트 탭의 `현재 조합 보기` 버튼도 커스텀 조합으로 이동하는 주요 액션이므로 로즈 와인 계열을 사용
- 편집용 모드에서 표시되는 패널, 필터, 카드, 텍스트, 액션은 같은 계열의 더 밝고 깊은 로즈 와인 변주로 특수 상태를 표현

### 5-3. 커스텀 콤보 관련 컬러셋
커스텀 콤보는 더 동적인, 생성적이고 실험적인 느낌을 줄 수 있는 따뜻한 오렌지/앰버 계열로 구분합니다. 조합보다 “개인화된 실험/변형” 느낌을 살리는 데 적합합니다.

| 토큰 | 의미 | 색상 |
| --- | --- | --- |
| custom-primary | 콤보 기본 강조 | #F59E0B |
| custom-secondary | 보조 강조 | #FBBF24 |
| custom-surface | 콤보 카드 배경 | #2A1F16 |
| custom-border | 콤보 경계 | #7C5223 |
| custom-soft | 연한 강조 배경 | rgba(245,158,11,0.12) |
| custom-text | 콤보 관련 텍스트 | #FDE68A |

사용 가이드:
- 실험적인 조합, 사용자 정의 큐레이션, 콤보 수정/변경 상태에 사용
- 갈색/오렌지 계열은 시선을 끌지만 남발하면 주의를 분산하므로 제한적으로 사용

### 5-4. 태그 관련 컬러셋
태그는 가장 작은 단위의 정보이지만, 시각적 우선순위가 낮으면서도 구분이 빠르게 되도록 만들기 위해 황금색 계열로 통일합니다. 개별 프롬프트와 달리 태그는 보조 식별용으로 수행되며, 주된 강조 색상이 아니라 작은 라벨처럼 보이게 설계합니다.

| 토큰 | 의미 | 색상 |
| --- | --- | --- |
| tag-primary | 기본 태그 | #D4A62F |
| tag-secondary | 연한 강조 | #F4C95D |
| tag-surface | 태그 배경 | #2B220E |
| tag-border | 태그 경계 | #7A5A1C |
| tag-soft | 연한 배경 | rgba(212, 166, 47, 0.14) |
| tag-text | 태그 텍스트 | #F9E7A7 |

사용 가이드:
- 카테고리, 키워드, 라벨, 필터 태그에 사용
- 태그는 중요한 내용보다 보조 식별 역할에 집중하므로, 너무 강렬하지 않은 골드 톤으로 유지
- 황금색 계열은 포인트를 만들되 개별 프롬프트 또는 조합의 강조 색상과 충돌하지 않도록 제한적으로 사용

---

## 6. 사용 규칙

### 6-1. 색상 분리 원칙
- 개별 프롬프트: 인디고 계열 단일 라인으로 통일
- 커스텀 조합: 로즈 와인 계열
- 커스텀 콤보: 오렌지 계열
- 태그: 황금색 계열

개별 프롬프트는 같은 계열에서 음영만 조절해 편집 상태, 선택 상태, 포커스 상태를 표현하며, 다른 콘텐츠 유형 간에 색상 경계를 명확히 유지합니다. 태그는 황금색 계열을 사용해 보조 메타 정보임을 빠르게 인지할 수 있도록 합니다.

### 6-2. 반복 사용 제한
- 각 화면에서 같은 중복 색상을 3개 이상 쓰지 않도록 제한
- 하나의 특정 영역에 2가지 포인트 컬러만 넘지 않게 유지
- 제목과 상태 표시에서는 포인트 컬러를 우선, 노이즈는 배제

### 6-3. 접근성 기준
- 텍스트 대비는 최소 WCAG 기준 이상을 유지
- 배경과 텍스트의 차이를 충분히 확보하고, 연한 배경 위 텍스트는 2단계 이상의 대비를 확보
- 포인트 컬러가 많은 경우에는 라벨 텍스트를 밝게 유지해 인지성을 높임

---

## 7. 향후 확장 방향

이 문서는 초기 기본 가이드이며, 이후 단계에서 아래 항목을 확장해 나갈 예정입니다.

1. 버튼 스타일 상세 세트
2. 폼 입력 및 모달 디자인
3. 사이드바/메인 보드 레이아웃 토큰
4. 카드 상태별 스타일 (default, hover, active, selected, disabled)
5. 아이콘 및 배지 시스템
6. 모바일/데스크탑 반응형 보정 규칙

---

## 8. 정리

현재의 앱은 이미 다크 기반의 고급 생산성 툴 느낌을 가지고 있으므로, 이번 리디자인은 전체 톤을 유지하면서 계열별 의미를 더 분명하게 만들어 가는 방식이 가장 적절합니다. 특히 보라/초록/오렌지/블루의 역할 분리는 UI를 이해하기 쉽게 만들고, 각 콘텐츠 영역을 빠르게 인식할 수 있게 합니다.

다음 단계에서는 이 가이드에 맞춰 실제 UI 요소를 재정리하고, 각 색상 세트가 실제 카드, 버튼, 태그에 어떻게 적용되는지 시각적으로 확장해 나가겠습니다.
